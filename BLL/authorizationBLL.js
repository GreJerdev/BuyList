var Groups = require('../models/groupModel');
var Items = require('../models/itemModel');
var Lists = require('../models/listModel');

module.exports = function () {

    function canSave(user, object,callback)
    {
           callback(err,true); 

    }

}